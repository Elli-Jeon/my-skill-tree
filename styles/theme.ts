import type { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
	// font color
	color: {
		black: "#424242",
		white: "#fafafa",
		gray: "#e0e0e0",
		main: "#303f9f", // blue
	},
};

export default theme;
